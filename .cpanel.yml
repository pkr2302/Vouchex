---
deployment:
  tasks:
    - export DEPLOYPATH=/home/kuhuorgi/vouchex
    - /usr/bin/rsync -a --exclude='.env' --exclude='vendor/' --exclude='storage/logs/' --exclude='storage/framework/cache/' --exclude='storage/framework/sessions/' --exclude='storage/framework/views/' --exclude='storage/app/public/' VouchEx/laravel-api/ $DEPLOYPATH/
